import { ComponentFunction } from './h';
export declare const withState: <T>(comp: ComponentFunction) => (initialState: T) => ComponentFunction;
export interface Keyable {
    key: number;
}
export declare const withIdempotentState: <T extends Keyable>(comp: ComponentFunction) => (initialProps: T) => ComponentFunction;
