import { SearchConfiguration, SearchFunction } from './interfaces';
export interface RegexSearchInput extends SearchConfiguration {
    escape?: boolean;
    flags?: string;
}
export declare const regexp: <T>(input: RegexSearchInput) => SearchFunction<T>;
