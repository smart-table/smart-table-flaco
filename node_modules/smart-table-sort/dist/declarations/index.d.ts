export declare const enum SortDirection {
    ASC = "asc",
    DESC = "desc",
    NONE = "none"
}
interface Comparator<T> {
    (a: T, b: T): number;
}
export interface SortConfiguration<T> {
    pointer?: string;
    direction?: SortDirection;
    comparator?: Comparator<T>;
}
export declare const defaultSortFactory: <T>(conf: SortConfiguration<T>) => (array: T[]) => T[];
export {};
