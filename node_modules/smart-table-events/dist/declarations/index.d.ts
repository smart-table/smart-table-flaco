export interface Emitter {
    on(event: string, ...listeners: Function[]): Emitter;
    dispatch(event: string, ...args: any[]): any;
    off(event: string, ...listeners: Function[]): any;
}
export declare const emitter: () => Emitter;
interface EventMap {
    [key: string]: string;
}
export interface ProxyEmitter {
    off(ev: string): ProxyEmitter;
}
export declare const proxyListener: (eventMap: EventMap) => ({ emitter }: {
    emitter: Emitter;
}) => ProxyEmitter;
export {};
